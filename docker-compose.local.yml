services:
  degoog-cli:
    build: .
    stdin_open: true
    tty: true
    environment:
      - DEGOOG_CONFIG_HOME=/degoog-config
    volumes:
      - "${HOME}/.config/degoog:/degoog-config"
      - "${OUTPUT_DIR:-.}:/workspace"
    working_dir: /workspace
